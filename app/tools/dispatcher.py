class Dispatcher:
    """
    Stub dispatch: would integrate with outbound messaging or CRM notes.
    """
    def send(self, case):
        # In production: send via secure channel, persist final draft in DB.
        pass
